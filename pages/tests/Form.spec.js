import * as React from "react";
import { mount } from "enzyme";

describe("Form spec", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
  it("should render an input", () => {
    expect(true).toBe(true);
  });
});
